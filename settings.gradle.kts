// ── settings.gradle.kts ─────────────────────────────────────────────────────
// Registra los sub-proyectos (plugins) que Gradle debe compilar.
// Agrega una línea "include(\"NombrePlugin\")" por cada plugin nuevo.

rootProject.name = "LatinoPlugins"

include("PelisplusHD")
include("Latanime")
